{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build checksum project",
      "type": "shell",
      "command": "g++",
      "args": ["tcp.cpp", "-lpcap", "-o", "tcp.out"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
