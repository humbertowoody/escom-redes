{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build sniffer project",
      "type": "shell",
      "command": "g++",
      "args": ["Sniffer.cpp", "-lpcap", "-o", "Sniffer.out"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
